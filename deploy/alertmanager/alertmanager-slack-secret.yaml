apiVersion: v1
kind: Secret
metadata:
  name: deploylens-alertmanager-slack
  namespace: monitoring
type: Opaque
stringData:
  # Replace with the real Slack incoming webhook URL.
  # In production, use `kubectl create secret` instead of this file.
  url: "REPLACE_WITH_SLACK_WEBHOOK_URL"

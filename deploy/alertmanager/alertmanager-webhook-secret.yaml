apiVersion: v1
kind: Secret
metadata:
  name: deploylens-alertmanager-webhook
  namespace: monitoring
type: Opaque
stringData:
  # Must match ALERTMANAGER_WEBHOOK_TOKEN in .env (ingest service).
  # In production, use `kubectl create secret` instead of this file.
  token: "REPLACE_WITH_WEBHOOK_TOKEN"

apiVersion: v1
kind: Secret
metadata:
  name: argocd-notifications-secret
  namespace: argocd
type: Opaque
stringData:
  # Must match ARGOCD_WEBHOOK_TOKEN in .env
  kubex-token: "REPLACE_WITH_ARGOCD_WEBHOOK_TOKEN_FROM_ENV"

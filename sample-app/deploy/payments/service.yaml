apiVersion: v1
kind: Service
metadata:
  name: payments
  namespace: deploylens
  labels:
    app: payments
spec:
  selector:
    app: payments
  ports:
    - name: http
      port: 8000
      targetPort: http

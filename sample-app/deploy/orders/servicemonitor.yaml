apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: orders
  namespace: deploylens
  labels:
    app: orders
spec:
  selector:
    matchLabels:
      app: orders
  endpoints:
    - port: http
      path: /metrics
      interval: 15s

apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: deploylens
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 8000
      targetPort: http
